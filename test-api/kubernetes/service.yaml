apiVersion: v1
kind: Service
metadata:
  namespace: test-api
  labels:
    app: test-api
  name: test-api
spec:
  ports:
  
    - protocol: TCP
      port: 9001
      targetPort: 9001
   
   
  selector:
    app: test-api
  type: ClusterIP
